{
  "name": "@larshp/scrypt-rs-wasm",
  "version": "0.0.1",
  "description": "todo",
  "main": "pkg/scrypt_rs_wasm.js",
  "scripts": {
    "pub_in_pkg": "npm --no-git-tag-version version patch && npm publish",
    "test": "rm -rf pkg && wasm-pack build --debug --target nodejs && node index.mjs"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/larshp/scrypt-rs-wasm"
  },
  "devDependencies": {
  },
  "author": "Lars Hvam",
  "license": "MIT"
}
